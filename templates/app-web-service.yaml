apiVersion: v1
kind: Service
metadata:
  name: app-web
  labels:
    io.kompose.service: app-web
spec:
  selector:
    io.kompose.service: app-web
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
